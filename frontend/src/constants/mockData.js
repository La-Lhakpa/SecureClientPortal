// DEV ONLY — REMOVE WHEN BACKEND AUTH IS READY
// Mock data for development. Replace with API calls when backend is ready.

/**
 * Mock list of recipient clients
 * TODO: Replace with API call to /users/clients
 */
export const MOCK_RECIPIENTS = [
  "client1@example.com",
  "client2@example.com",
  "client3@example.com",
];
